# References

This directory holds reference materials that may be useful when building the VAIC Universal Starter.

## Contents

Currently empty. Add reference documents here as needed.

## Recovery

The old AI Agent template is preserved in the `backup/old-agent-template` git branch.
To view old architecture decisions or implementation patterns:

```bash
git checkout backup/old-agent-template -- path/to/file
```

## Guidelines

- Do NOT copy old agent-specific code into the new template.
- Reference documents should inform design decisions, not dictate implementation.
- Remove references that are no longer relevant.
