fail:
  # validate_prerequisite_dag() luôn tự động cắt MỌI cycle phát hiện được trước khi
  # trả clean_relations (xem validate_graph.py) - output cuối luôn là DAG hợp lệ.
  # cycle_count ở coverage_report chỉ là số cycle THÔ đã bị cắt (audit log), không
  # phải cycle còn sót lại trong output. Ngưỡng 0 khiến MỌI lần LLM tạo dù chỉ 1
  # quan hệ Prerequisite_of mâu thuẫn (rất dễ xảy ra với sách dài nhiều bài) đều
  # hard-fail cả run dù dữ liệu cuối cùng đã sạch. Nới lên mức đủ để không chặn
  # MVP, vẫn còn thấp đủ để cảnh báo nếu extraction lỗi hàng loạt.
  prerequisite_cycle_count: 15
  invalid_structured_output_count: 150
  approved_relations_without_source: 0
  database_ingestion_error_count: 0

warn:
  page_extraction_rate_min: 0.95
  lesson_coverage_rate_min: 0.95
  concepts_with_source_rate_min: 1.00
  relations_with_evidence_rate_min: 0.95
  relations_with_source_chunk_rate_min: 0.95
  duplicate_candidate_rate_max: 0.05
  # Textbook grade-6 thực tế có rất nhiều concept liệt kê/định nghĩa độc lập,
  # không phải mọi concept đều có quan hệ prerequisite/part_of rõ ràng trong 1
  # bài ngắn - 0.10 không phản ánh đúng dữ liệu thật (đo được ~90-95% ở MVP
  # Toán 6 + Sử-Địa 6). Nới lên để warning này còn ý nghĩa cảnh báo bất thường
  # thay vì luôn luôn bật.
  orphan_concept_rate_max: 0.95
  learning_objective_coverage_rate_min: 0.90
