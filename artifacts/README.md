# Artifacts Directory

This folder holds output results, generated reports, models, or processing run products in production.
 during local runs, the storage service defaults to caching artifacts under `./data/artifacts/`.
